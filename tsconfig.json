{
  "compilerOptions": {
    /* 基础设置 */
    "target": "ES2022",                          // 编译到 ES2022（支持 top-level await, import.meta 等）
    "module": "NodeNext",                        // 使用 Node.js 原生 ESM（.js + .mjs 风格）
    "moduleResolution": "NodeNext",              // 模块解析策略匹配 Node.js ESM
    "outDir": "./dist",                          // 编译输出目录
    "rootDir": "./src",                          // 源代码目录

    "types": ["@cloudflare/workers-types"],

    /* 严格性 */
    "strict": true,                              // 启用所有严格类型检查
    "esModuleInterop": true,                     // 允许 import * as fs from 'fs' 等 CommonJS 互操作
    "skipLibCheck": true,                        // 跳过 .d.ts 库文件的类型检查（加快编译）
    "forceConsistentCasingInFileNames": true,    // 防止大小写混淆（尤其在 Windows 上）

    /* 其他 */
    "declaration": false,                        // 不生成 .d.ts 声明文件（除非你要发布库）
    "sourceMap": true,                           // 生成 source map，便于调试
    "removeComments": false                      // 保留注释（可选）
  },
  "include": ["src/**/*"],                       // 包含 src 下所有 TS 文件
  "exclude": ["node_modules", "dist"]            // 排除这些目录
}